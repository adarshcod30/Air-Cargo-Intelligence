# Serving dependencies only.
#
# This is the deployed read path, not the development environment. The
# ingestion and analytics stages need pdfplumber, pandas, statsmodels and
# scipy; together those exceed the serverless function size limit, and the
# API imports none of them because forecasts and anomalies are read back as
# rows rather than recomputed per request.
#
# For local development install the full set instead:
#     pip install -e ".[dev]"

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg[binary]>=3.2
pydantic>=2.9
python-dotenv>=1.0
# Titan embeddings for query-time retrieval, and the reasoning layer.
boto3>=1.35
